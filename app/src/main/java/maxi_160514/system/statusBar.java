package maxi_160514.system;

public class statusBar {

	/*public void sendNews(Context context, String text) {// ******************系统通知消息显示

		NotificationManager nm;
		String service = Context.NOTIFICATION_SERVICE;
		nm = (NotificationManager) context.getSystemService(service); // get

		Notification n = new Notification();
		n.icon = R.drawable.up;
		n.tickerText= text;
		n.when = System.currentTimeMillis();

		Intent intent = new Intent(context, edit.class);

		PendingIntent pi = PendingIntent.getActivity(context, 0, intent, 0);
		n.setLatestEventInfo(context, "title", "content", pi);
		nm.notify(1, n);
		nm.cancel(1);

	}*/

}
